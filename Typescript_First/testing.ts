console.log('helllo');

const age=25
console.log();

export {}                                     //Adding export {} turns the file into a module, scoping all its variables to the file itself
                                              //let can be used in block scope({}) that's why using it

